*For starting the app:
  First Install packages inside requirements.txt
  then run python maze.py (name of your maze file e.g:maze1.txt)
* After running the app you should see the solution in terminal and maze.png will show you the visualized solution where yellow tiles are the graph it found and red tiles are all the explored nodes
